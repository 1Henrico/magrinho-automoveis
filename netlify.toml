[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

# Optional: custom headers for caching (uncomment and tweak if needed)
; [[headers]]
;  for = "/*"
;  [headers.values]
;    X-Frame-Options = "DENY"
